# Worklog 2026-06-29

## Reliability and data-health cleanup

- Added `data-health-fix` to quarantine corrupt paper rows with invalid
  `decimalOdds <= 1`.
- Cleared post-kickoff CLV captures from calibration inputs so the CLV report
  measures the intended pre-kickoff closing window.
- Fixed the calibration bootstrap so ROI/CLV confidence intervals are no longer
  degenerate.
- Current main model remains `RESEARCH_ONLY`: CLV is promising, but settled
  h2h VALUE sample is still below the 200-bet readiness gate.

Current key report state after refresh:

- Primary h2h VALUE settled: 84 / 200.
- Primary h2h VALUE CLV: 142 / 200.
- VALUE h2h ROI: about +6.2%, but sample is still too small for staking.
- VALUE h2h average CLV: about +1.05%.
- ROI confidence interval is still wide, so ROI is not proven.
- Live WebSocket remains diagnostic only because it still has zero stable odds
  market messages.
- `/odds/updated` fallback remains the useful live measurement path.

## Main 2-API Telegram scanner

Decision:

- Keep the strict 2-API scanner for Stoiximan/Pamestoixima.
- Keep World Cup active through `config/multisport-map.json`.
- Lower/keep the confirmed Telegram floor at 5% EV, not 10%.
- Do not send Telegram for paper-only rows, totals research rows, unsupported
  markets, stale candidates, or rows that fail Pinnacle/consensus confirmation.

Telegram alert criteria:

1. Odds-API.io finds a Stoiximan or Pamestoixima candidate.
2. Candidate market is full-event `MATCH_RESULT` / h2h / 1X2.
3. Candidate provider EV is at least 5%.
4. The event is pre-match and the quote is fresh.
5. The league maps cleanly to an active The Odds API sport key.
6. The Odds API can match the same event and exact selection.
7. Pinnacle de-vigged EV is strictly above 5%.
8. Median consensus EV from at least three complete reference books is strictly
   above 5%.
9. Candidate has not already been sent.
10. Telegram delivery succeeds.

Observed result:

- `reports/mispricing-alerts.csv` contains two sent World Cup Stoiximan alerts
  from 2026-06-26.
- `telegram-test` was run on 2026-06-29 and returned `message 9`; the user
  confirmed receiving it. Telegram delivery is therefore working.
- Recent quiet periods are mainly because the scanner did not find new clean
  confirmed +5% h2h opportunities, not because Telegram was broken.

## Odds-API.io free-tier usage

Decision:

- Use unused free-tier Odds-API.io capacity for measurement, not looser alerts.
- Added an Odds-API.io value sampler that writes raw observations to
  `reports/oddsio-value-sampler.csv`.
- Added a scheduled `/odds/updated` fallback poller for live measurement.
- These tasks do not send Telegram and do not spend The Odds API credits.

Reason:

- Raw sampler data helps coverage and bookmaker-distribution analysis.
- It should not bypass the strict Pinnacle/consensus confirmation gate.

## Betsson one-API POC

Decision:

- Keep the Betsson single-provider experiment isolated in the
  `codex/betsson-single-api-poc` worktree.
- Use The Odds API only, with Betsson as the candidate book and other books on
  the same event id as consensus.
- Include World Cup in the scheduled scan.
- Enable Telegram only as h2h watchlist when `--telegram-watchlist` is present.
- Keep `TOTALS`, `BTTS`, `DRAW_NO_BET`, and `DOUBLE_CHANCE` research-only.

Reason:

- One API removes cross-provider event matching errors.
- It is cleaner software architecture, but it only covers books available in
  The Odds API and still needs separate Betsson CLV/ROI evidence.

## Current operational stance

- Alerts are watchlist-only.
- No auto-betting.
- No staking language.
- No production readiness until the gates pass:
  - at least 200 h2h VALUE CLV samples,
  - at least 200 h2h VALUE settled bets,
  - VALUE CLV positive,
  - VALUE beats CONTROL,
  - ROI lower bound not clearly negative,
  - usable live/liquidity evidence.

## Repair & reliability plan — Phases 0–2 (afternoon)

Executed the approved phased plan (`~/.claude/plans/sunny-coalescing-dahl.md`).
Live decisioning unchanged throughout: same `confirmCandidate`, 5% floor, payload.
Test suite grew 293 → **313 green**, 0 API credits spent.

**Phase 0 — hygiene.**
- Committed the two pending correctness fixes: `alertIdentity` dedup by matched
  reference event (`d3e1167`); `pamestoixima` excluded from consensus depth (`17ff6e0`).
- `reports-prune [--keep=N]` retention command + `reports_prune.mjs` (`c3b3755`).

**Phase 1 — reliability of the measurement (highest priority).**
- `espn-settle` (`8ea64bf`): free, keyless ESPN public-scoreboard settlement for
  leagues football-data.org does not cover (NFL/NCAAF/NBA/WNBA/MLB/AFL + non-fd
  soccer: Libertadores/Sudamericana/China SL/Brazil B). Wired into
  `run-paper-settle.ps1` after `fd-settle`, before the credit-spending `settle`.
- `outcome-calibration-report` (`offline`): Brier/log-loss/classwise-ECE/reliability
  + win-rate-vs-implied gap on our **own** settled outcomes, bucketed by tier/odds/
  market/sport. First read on the real ledger: VALUE h2h n=84 win 60.7% vs implied
  53.7% (**gap +7.0%**, ECE 0.179), CONTROL gap +4.0% → honest separation ~3pp.
- Decision report now shows Outcome-calibration + Settlement sections and the
  forward guards `SETTLEMENT_COVERAGE_LOW` / `VALUE_OUTCOME_MISCALIBRATED`.

**Settlement reality check (important).** Of 1,302 PENDING, only ~99 are past/
playable; the rest are **future fixtures** (World Cup in progress, NFL/NCAAF
upcoming). The genuinely stuck ones are boxing/cricket/KBO/NPB — sports ESPN does
not two-team-score — so ESPN's value is **forward** (settle as games are played,
free), not draining today. The 200-gate is gated by time, not by a settle tool.

**Phase 2 — paper methods, offline only.**
- `value.mjs`: `devigOoEpc` (Goto Alg.5) + `devigFlGlm` (Goto Alg.6).
- Historical calibration now scores `oo_epc` and a train-fitted `fl_glm` (no
  leakage) and reports RPS, classwise-ECE, expected-vs-actual draw counts (the
  Goto/Baboota draw-bias check). 4th-decimal expected; never live-promoted. (`<commit>`)

**Phase 3 (cli.mjs split) deferred** to a dedicated session — large structural
refactor; first increment should extract the boost commands into `src/commands/`.

## Dataset-quality follow-up (evening)

- **Credits checked (free `/v4/sports` call): 1,896 / 20,000 left** until the
  1 Jul reset. Historical pulls (~3,800 cr) are not affordable until after reset.
- **Way B implemented:** `scan_scope.mjs` — the scan no longer generates paper
  bets for sports with neither free settlement nor a soft-book edge (boxing, MMA,
  cricket, KBO, NPB); `--include-all-sports` overrides. Higher signal-to-noise,
  free. 316 tests green.
- **Way A (multi-snapshot CLV) NOT implemented — deliberate.** Reading the code,
  CLV is intentionally single-capture (`applyClosingLine` + test "does not
  overwrite an existing CLV capture"), and extra Pinnacle snapshots cost The Odds
  API credits we don't have until 1 Jul. The better version is to capture the
  SOFT book's own line movement via Odds-API.io `GET /odds/movements` (its quota,
  not the 20k), which measures edge decay directly. Queued for after the reset.
- **Odds-API.io historical clarified:** it carries the soft books LIVE but exposes
  only `/odds/movements` (per-event movement trail), not a deep season archive —
  so it still cannot backtest past seasons of soft-vs-sharp; The Odds API historical
  has the deep archive but no soft books. Neither can backtest the real edge.
