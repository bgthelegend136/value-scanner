# Worklog 2026-06-29

## Betsson one-API Telegram/watchlist update

- Kept the Betsson experiment isolated in `codex/betsson-single-api-poc`.
- The scheduled Betsson one-API scan now includes `soccer_fifa_world_cup`.
- The scheduled runner passes `--telegram-watchlist`, so clean Betsson h2h
  findings can reach Telegram.
- The scan remains The Odds API only and does not require `ODDS_API_IO_KEY`.
- `TOTALS`, `BTTS`, `DRAW_NO_BET`, and `DOUBLE_CHANCE` remain research-only and
  do not send Telegram.

Telegram/watchlist rule:

1. Bookmaker must be `betsson`.
2. Market must be h2h / `MATCH_RESULT`.
3. The event must be in the configured sports list, including World Cup.
4. EV must be at least 5% and no more than 25%.
5. The command must run with `--telegram-watchlist`.
6. Consensus depth must be at least 4 books.
7. Kickoff must be 1-72 hours away and the quote must be fresh.
8. There is no staking language until Betsson-specific CLV/settlement gates pass.

Reason:

- The one-API architecture removes cross-provider event-matching errors.
- It still needs its own Betsson evidence before it can be treated as more than
  a watchlist experiment.
