# Ημερολόγιο εργασιών — 2026-06-27

Προστέθηκε **δωρεάν settlement μέσω football-data.org** ώστε το `settle` να μη
ξοδεύει The Odds API credits για soccer/Μουντιάλ. TDD, 197 tests πράσινα.

## Σύνοψη

Ο πραγματικός φραγμός είναι τα **500 credits/μήνα της The Odds API**· το settle
έτρωγε ~150/μήνα μόνο για τελικά σκορ (commodity data). Νέα εντολή **`fd-settle`**
settle-άρει soccer bets **δωρεάν** μέσω football-data.org (X-Auth-Token, 10 req/min).
Το CLV (closing line Pinnacle) μένει στην The Odds API — δεν αντικαθίσταται.

## Τι έγινε

- `src/football_data_client.mjs` — minimal v4 client· διαβάζει το throttling header
  `X-Requests-Available-Minute` (οδηγία TheSportsDB/FD), δεν διαρρέει το key σε errors.
- `src/football_data_settle.mjs` — map sportKey→competition (WC, PL, SA, BSA, …) +
  `synthesizeFdScoreEvents` (ταίριασμα ανά normalized ονόματα + ημερομηνία, με
  fallback unique-names· skip σε ambiguous/unfinished → fail safe).
- `src/cli.mjs` — νέα εντολή `fd-settle` (settle-άρει μόνο τις FD-καλυπτόμενες soccer
  λίγκες· οι υπόλοιπες μένουν για το `settle`/The Odds API). Loader για
  `football_data_org_key`.
- `scripts/run-paper-settle.ps1` — τρέχει **`fd-settle` ΠΡΩΤΑ** (free), μετά `settle`
  (Odds API για non-soccer) + `mispricing-settle`.
- +7 tests (`football_data.test.mjs`, `cli_fd_settle.test.mjs`). Σύνολο **197**.

## Live επαλήθευση (2026-06-27)

`node src/cli.mjs fd-settle` → `WC requests available: 9`, **settled 4 bets (3W-1L),
12 pending**, **μηδέν The Odds API credits**. Καθώς τελειώνουν τα ματς, settle-άρονται
δωρεάν.

## Σημειώσεις / εκκρεμότητες

- **TheSportsDB:** το key `sports_db_key` υπάρχει, αλλά το v1 probe (`eventspastleague
  id=4429`) έδωσε HTTP 400 — θέλει σωστό league id ή v2 (X-API-KEY header). Χρειάζεται
  μόνο για **non-soccer** (MLB/NFL/NPB) free settlement· αφέθηκε για επόμενο βήμα.
- **SGO:** απόφαση "defer" (HANDOFF §10) — US-centric, περιττό· bottleneck = EV, όχι coverage.

## Token-budget management (3-day window)

Τρέχον quota: **~280/500** The Odds API. Ενέργειες:
- **Quota guard:** `runScan` σταματά νωρίς αν το The Odds API quota πέσει κάτω από
  `MIN_SCAN_QUOTA = 60` (cli.mjs) — ο autoscan δεν μπορεί να αδειάσει το budget. +1 test (198).
- **`Bet-Mispricing-Funnel` → Disabled** (διπλασίαζε provider calls, έκαιγε quota).
- **`Bet-Paper-Scan` kill switch:** η επανάληψη (8h) **αυτο-σταματά ~2026-06-30**
  (RepetitionDuration 3 μέρες). Για restart: ξανατρέξε `install-paper-scan-task.ps1`.
- **`Bet-Paper-Settle`:** enabled (07:30 ημερήσιο)· τρέχει `fd-settle`(free)→`settle`→
  `mispricing-settle`· επιβεβαιώθηκε live (Result 0).

## TheSportsDB — discovery: ΔΕΝ είναι βιώσιμο στο free tier

Το `sports_db_key` διορθώθηκε σε **`123`** (το σωστό free key — το screenshot το έδειξε).
Όμως το **free tier είναι πολύ περιορισμένο**: `eventsday`/`eventspastleague` γυρίζουν
~3 events/μέρα (1 για MLB), και το v2 θέλει premium ("Invalid Premium API key").
**Δεν αξιόπιστο για settlement.** Απόφαση: **deferred** — χρειάζεται TheSportsDB
Premium (~$10/μήνα) για να αξίζει. Μέχρι τότε το non-soccer settle μένει στο The Odds
API (φθηνό, και δεν υπάρχουν non-soccer bets ακόμα). `football-data.org` καλύπτει όλα
τα τωρινά (soccer) δωρεάν.

## Free settlement provider probe — API-Sports / Highlightly

Ο ιδιοκτήτης πρόσθεσε `api_sports_key` και `highlightly_key`. Έγινε live probe χωρίς
να τυπωθεί κανένα secret.

**API-Sports: προτιμώμενος υποψήφιος για Part B.**
- Football `/fixtures?date=2026-06-26`: HTTP 200, 173 rows, limit 100/day, sample `FT`
  με `score.fulltime`.
- Baseball `/games?date=2026-06-26`: HTTP 200, 53 rows, sample `FT` με
  `scores.home.total` / `scores.away.total`.
- Basketball `/games?date=2026-06-26`: HTTP 200, 22 rows, sample `FT` με totals.
- Hockey `/games?date=2026-06-26`: HTTP 200, 1 row.

**Highlightly: backup, όχι πρώτο.**
- Baseball/basketball/hockey direct hosts δούλεψαν με 100/day limit.
- Baseball/basketball έχουν usable finished score rows όταν φιλτράρεις σωστά by date/status.
- Hockey δεν είχε rows στην tested previous date.
- Το documented football direct host (`football.highlightly.net`) έδωσε DNS failure.

Απόφαση: δεν αγοράζουμε TheSportsDB Premium τώρα. Αν εμφανιστούν αρκετά non-soccer
paper bets ή αρχίσει να καίει credits το The Odds API settlement, χτίζουμε πρώτα
API-Sports settlement adapter με TDD. Highlightly μένει ως fallback/probe.

## Stoiximan + Novibet switch

Ο ιδιοκτήτης άλλαξε το Odds-API.io bookmaker access σε **Stoiximan + Novibet**.
Live probe χωρίς secrets:
- `Stoiximan`: HTTP 200, 99 rows.
- `Novibet`: HTTP 200, 97 rows.
- `Superbet`: HTTP 403.

Αλλαγές:
- Paper `scan` / capture path ζητά πλέον `Novibet, Stoiximan`.
- Live `mispricing-scan` ζητά πλέον `Stoiximan, Novibet`.
- `mispricing-funnel` diagnostic script ζητά πλέον `Stoiximan, Novibet`.
- Novibet support στο strict normalizer με allowlist: `novibet.gr`,
  `www.novibet.gr`, `novibet.bet.br`.
- `novibet` μπήκε στο excluded consensus set, ώστε να μη μετρά ως ανεξάρτητο
  reference όταν ελέγχουμε Novibet candidate.

Verification:
- TDD red στα bookmaker expectations, μετά implementation.
- `node --test` -> 198/198 passing.
- Live paper `node src/cli.mjs scan`: 10 leagues, 32 matched fixtures, 6 value
  bets, **4 νέα paper bets**, 2 duplicates, quota 260.
- `node src/cli.mjs clv`: 19 captured, 18 positive, beat-rate 94.7%, avg CLV +2.2%,
  quota 258.
- `node src/cli.mjs mispricing-scan --dry-run`: candidates 1, mapped 0,
  confirmed 0, sent 0. Άρα κανένα Telegram-worthy strict alert τώρα.

## Part C — data window + P4 audit snapshot

Συνέχιση χωρίς αλλαγή thresholds και χωρίς νέο runtime code.

**Scheduler / quota state**
- `Bet-Paper-Scan`: Ready, last result 0, next run 2026-06-27 08:00, repetition
  `PT8H` με `Duration=P3D` και `StopAtDurationEnd=true`.
- `Bet-Paper-Settle`: Ready, daily 07:30, last result 0.
- `Bet-Mispricing-Funnel`: Disabled.
- Heartbeat: last success `2026-06-26T23:30:03Z`, quota remaining `280`.

**Current sample**
- Paper ledger: 16 total, 4 WON, 1 LOST, 11 PENDING.
- `clv-report`: 15 captured, 14 positive, beat-rate 93.3%, average CLV +2.2%.
- Calibration remains blocked: sample is too small and still mostly/entirely World Cup.

**P4 audit**
- `mispricing-audit.csv`: 30 `UNMAPPED_SPORT_LEAGUE` rows.
- Largest groups: club friendlies (6), Chile Copa Chile Group C (6), New Zealand
  Southern League (4), Brazil Serie D knockout (2), plus scattered lower
  Australia/Argentina/Chile/Tennis/Esports rows.
- Zero-credit The Odds API `/sports` check returned 45 active sports and no active
  matching keys for these lower leagues (only Brazil Serie A/B and WTA tennis were
  near textual matches). No mapping was added; forcing one would create bad matches.

**Next trigger**
- Let the 3-day window continue to the auto-stop.
- After more rows: rerun `clv-report` and the P4 audit.
- Build API-Sports settlement only if non-soccer paper bets appear or The Odds API
  settlement burn becomes material.

## Paper CLV timing + value-flow diagnostics

Quant/model follow-up after reviewing the low bet count:
- Low paper/live volume is mostly explained by narrow coverage: two target books,
  mapped active leagues, mostly MATCH_RESULT, duplicate suppression, and the 10%
  live-alert EV gate. No evidence of a broken EV formula from the current reports.
- Fixed a calibration-quality issue: paper `clv` now captures only when kickoff is
  inside the 20-minute CLV window. Rows outside the window cause zero The Odds API
  calls and remain pending for a later run.
- Split paper scheduling responsibilities: `run-paper-scan.ps1` runs only `scan`;
  new `run-paper-clv.ps1` and `install-paper-clv-task.ps1` register a 15-minute
  `Bet-Paper-CLV` task. It was installed live and is Ready; first next run observed
  at 2026-06-27 03:15 local time.
- Added `value-flow-report`, a no-quota local report over existing CSVs
  (`paper-bets.csv`, `mispricing-audit.csv`, latest `scan-all-*.csv`) to summarize
  paper totals, CLV capture, rejection reasons, latest scan EV flow, and book/sport
  distribution.
- Added `scan --bookmakers=A,B` for paper-only bookmaker experiments. It does not
  change `mispricing-scan` / Telegram target books or the 10% live floor.
- Verification: focused TDD tests passed, PowerShell syntax parse passed for the
  new scripts, `node --test` -> 203/203 passing. Live local `value-flow-report`
  snapshot: paper=20, audit=2518, latestScanRows=174, top rejection
  `CANDIDATE_EV_BELOW_MIN`=2334.

## P4 paper coverage mapping

Followed the Cursor/Odds-API.io probe recommendation, but kept the change
paper-only and fail-closed:
- Added 8 exact registry mappings to `config/multisport-map.json`:
  EFL Championship, League One, League Two, Norway Eliteserien, Sweden
  Allsvenskan, DFB-Pokal, Finland Veikkausliiga, and WNBA.
- Did not add Wimbledon yet; the probe identified the value-bets slug but did
  not verify the matching `listEvents` league slug.
- Did not change Telegram, live `mispricing-scan`, the 10% floor, market
  support, CLV timing, or settlement behavior.
- Fresh The Odds API `/sports` check confirmed all 8 reference keys active and
  cost 0 quota: `soccer_efl_champ`, `soccer_england_league1`,
  `soccer_england_league2`, `soccer_norway_eliteserien`,
  `soccer_sweden_allsvenskan`, `soccer_germany_dfb_pokal`,
  `soccer_finland_veikkausliiga`, `basketball_wnba`.

## Historical-subscription analysis + go/no-go probe

Ο ιδιοκτήτης εξετάζει 1 μήνα paid The Odds API για **historical odds** ώστε να
μαζέψει calibration samples πιο γρήγορα. Ανάλυση + probe, χωρίς αγορά ακόμα.

**Κρίσιμα ευρήματα (από κώδικα + δεδομένα):**
- Το historical του The Odds API **δεν έχει Stoiximan/Novibet** → αδύνατο historical
  backtest της πραγματικής στρατηγικής (soft-book mispricing). Μπορεί μόνο
  **fair-value/devig calibration** του sharp side.
- **Off-season παγίδα:** EPL/Bundesliga/Serie A δεν παίζουν τώρα (Ιούν). Forward
  paper δεν μπορεί να μαζέψει domestic-league samples μέχρι Αύγουστο — άρα historical
  είναι ο μόνος τρόπος για samples αυτών των λιγκών *τώρα* (πάρε ολοκληρωμένη σεζόν 25/26).
- Επιβεβαίωση bottleneck (audit): `CANDIDATE_EV_BELOW_MIN`=2334/2518. Τα ≥10% edges
  είναι **δομικά σπάνια** (max ~4%). Λίγα bets = πραγματικότητα, όχι bug.
- Consensus depth εξαιρετικό: **median 25 books/opportunity** → το devig δεν στερείται input.
- **Pricing (verified):** historical featured = 10 credits/region/market/snapshot·
  player props event-level (ακριβά). Cheapest paid **20K=$30**, 100K=$59· όλα paid
  tiers έχουν historical. To paid plan δίνει επίσης **πολύ περισσότερα live credits**
  από το free 500 → επιταχύνει το πραγματικό bottleneck (forward paper), όχι μόνο historical.

**Probe (`scripts/historical-probe.mjs`, νέο, read-only, 0 credits by default):**
- Cost model (verified pricing): feasibility 1-league/~1-μήνας ≈ **800 cr ($30 plan)**·
  lean ½-season ≈ 3800 cr ($30)· target 3-leagues/full-season ≈ 22,800 cr ($59).
- Coverage από υπάρχοντα reports (0 credits) + opt-in `--live` recon (Pinnacle %,
  median books, totals %, soft-book absence) σε currently-active proxy leagues.
- Decision checklist με τον κανόνα: ο ΜΟΝΟΣ έγκυρος στόχος του spend = devig
  calibration (Brier/log-loss/reliability, OOS temporal split) ώστε να κατέβει ο 10%
  floor με στοιχεία. Το edge παραμένει **forward-CLV** ερώτημα.
- 203/203 tests πράσινα (νέο script, καμία αλλαγή σε src/tests).

## Paid plan ενεργό + roadmap (The Odds API 20K + WebSocket trial)

Ο ιδιοκτήτης **αγόρασε το The Odds API 20K plan (€30/μήνα)** → historical odds + 40×
τα παλιά free credits (20.000 vs 500). Παράλληλα, το Odds-API.io έδωσε **2ήμερο
δωρεάν WebSocket add-on**. Επιβεβαιώθηκαν δύο πραγματικές ανάγκες: fleeting odds
(Stoiximan Ιράκ @17.0 → ~12.0 μέσα σε λεπτά) και μελλοντικό live betting (P8).

**Νέο roadmap (εγκεκριμένο):** `docs/superpowers/plans/2026-06-27-post-paid-plan-roadmap.md`.
Τέσσερα workstreams, **όλα paper/measurement-only**, κανένα δεν αγγίζει το 10%
Telegram floor ή στοιχηματίζει:
- **A — 20K credits:** χαλάρωσε τα quota guards (`MIN_SCAN_QUOTA` σε `cli.mjs`,
  `QUOTA_RESERVE` σε `mispricing_scan.mjs`· κράτα ~1.000 cr για CLV) + ξανα-ενεργοποίησε
  συνεχές `Bet-Paper-Scan` → πιο γρήγορα τα ~200 settled CLV bets.
- **B — historical de-vig calibration (lean, 1 λίγκα ½ σεζόν ≈ 3.800 cr):** νέο
  `getHistoricalOdds` στο `theodds_client.mjs` + `scripts/historical-calibration.mjs`
  (reliability/Brier/log-loss, OOS split). **Pre-flight:** football-data.org free να
  καλύπτει το outcome window ΠΡΙΝ το ξόδεμα. ΔΕΝ είναι strategy backtest (το historical
  δεν έχει soft books). Καταχωρήθηκε ως **P9** στο backlog.
- **C — WebSocket measure-only:** `scripts/ws-lifetime-probe.mjs` (Node 22 built-in
  `WebSocket`, καμία dep) που καταγράφει lifetimes των fleeting edges στο 2ήμερο,
  decision rule buy/don't-buy. P8 first step.
- **D — live betting (P8):** scope-only groundwork (faster in-play alerts στον άνθρωπο,
  ΠΟΤΕ auto-betting).

**Verified pricing/limits** (στο memory + probe): historical featured = 10 cr/region/
market/snapshot· cheapest paid 20K=$30, 100K=$59. Zero-credit go/no-go probe +
cost model υπάρχει ήδη: `scripts/historical-probe.mjs`.

Ενημερώθηκαν: `HANDOFF-CODEX.md` §2 banner + §6 (P8 σημείωση, νέο P9), και αυτό το
WORKLOG. **Τίποτα από το roadmap δεν έχει υλοποιηθεί ακόμα** — είναι το επόμενο βήμα.
203/203 tests πράσινα.

Verification and runtime snapshot:
- TDD RED: `node --test provider-harness\test\multisport_map.test.mjs` failed
  on missing `football|england-championship`.
- GREEN: same test passed after adding the mappings.
- Live paper `node src/cli.mjs scan`: 18 in-season leagues, 35 matched
  fixtures, 6 value bets, 2 new paper bets, 4 duplicates, The Odds API quota
  remaining 221.
- `node src/cli.mjs value-flow-report`: paper=22, audit=3943,
  latestScanRows=159, top rejection `CANDIDATE_EV_BELOW_MIN`=3731.

## Εκτέλεση post-paid roadmap από Codex

Υλοποιήθηκαν τα βασικά paper/measurement-only κομμάτια του roadmap χωρίς αλλαγή
στο 10% Telegram floor και χωρίς auto-betting.

| Workstream | Κατάσταση | Σημείωση |
| --- | --- | --- |
| A - 20K credits | DONE | `MIN_SCAN_QUOTA` και `QUOTA_RESERVE` έγιναν 1000 credits, `Bet-Paper-Scan` πήγε σε 4h χωρίς 3-day stop. |
| B - historical calibration | DONE για harness + tiny run | Προστέθηκε `getHistoricalOdds` και `scripts/historical-calibration.mjs`; full half-season pull μένει συνειδητά επόμενο βήμα. |
| C - WebSocket measure | DONE για instrument + smoke | `scripts/ws-lifetime-probe.mjs` συνδέθηκε με redacted key και welcome Stoiximan/Novibet· full lifecycle θέλει longer run. |
| D - P8 scope | DONE | Νέο scope doc: `docs/superpowers/plans/2026-06-27-p8-live-betting-scope.md`. |

### Τι άλλαξε

- Το paper `scan` σταματά πλέον όταν The Odds API quota πέσει κάτω από 1000
  credits, ώστε να μένει CLV reserve στο paid plan.
- Το strict `mispricing-scan` κρατά το ίδιο 1000-credit reserve πριν κάνει νέα
  reference verification. Δεν άλλαξε το alert floor.
- Το `install-paper-scan-task.ps1` επαναλαμβάνει κάθε 4 ώρες και δεν έχει
  3-day auto-stop. Το runner παραμένει paper-only και Telegram-free.
- Το historical harness κάνει πρώτα football-data.org outcome preflight. Το EPL
  `PL` για Aug-Dec 2025 γύρισε 0 rows, οπότε δεν ξοδεύτηκαν historical credits
  εκεί. Χρησιμοποιήθηκε La Liga `PD`, που είχε 171 finished matches.
- Το tiny historical dry-run για 1 match ολοκληρώθηκε και έγραψε
  `reports/historical-calibration-2026-06-27T05-36-38.243Z.json/csv`.
- Το WebSocket probe χρησιμοποιεί Node 22 built-in `WebSocket`, `seq`/`lastSeq`
  και δεν τυπώνει ποτέ το API key. Επειδή το WS odds payload δεν έχει EV, το v1
  γράφει high-price lifetime windows και αφήνει `providerExpectedValue` κενό.

### Live verification

- `npm test` / `node --test`: **210/210 passing**.
- `node scripts/historical-calibration.mjs --preflight-only --from=2025-08-01 --to=2025-12-31`:
  `PD` returned 171 finished matches.
- `node scripts/historical-calibration.mjs --from=2025-08-01 --to=2025-12-31 --max-matches=1 --bins=5`:
  report written, The Odds API quota remaining 19900.
- `node scripts/ws-lifetime-probe.mjs --duration-minutes=0.05 --sport=football --status=prematch --markets=ML --min-odds=15`:
  connected, welcome bookmakers Stoiximan/Novibet, no lifecycle expected in 3 seconds.
- `node src/cli.mjs scan`: 18 in-season leagues, 35 matched fixtures, 7 value bets,
  1 new paper bet, 6 duplicates, quota remaining 19864.
- `node src/cli.mjs clv`: no paper bets inside CLV capture window, zero spend.
- `node src/cli.mjs value-flow-report`: paper=25, audit=4297, latestScanRows=157,
  top rejection `CANDIDATE_EV_BELOW_MIN`=4078.

### Εκκρεμότητες

- Να γίνει longer WebSocket run για πραγματικό appear→disappear lifecycle και
  απόφαση buy/don't-buy.
- Να τρέξει το full historical calibration (`--full`) μόνο αφού ελεγχθεί το tiny
  report και γίνει αποδεκτό το credit spend.
- Δεν προστέθηκαν νέα P4 mappings: τα τρέχοντα `UNMAPPED_SPORT_LEAGUE` groups
  δεν είχαν active The Odds API `/sports` match.
