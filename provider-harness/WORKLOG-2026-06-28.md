# Worklog 2026-06-28

## Forward CLV calibration

- Added `node src/cli.mjs clv-calibrate`.
- The command is offline-only: it reads `reports/paper-bets.csv`, excludes rows
  without valid numeric `ev` and captured `clv`, and writes:
  - `reports/clv-calibration.csv`
  - `reports/clv-calibration.json`
- Report dimensions:
  - overall
  - EV bucket (`<-5%`, `-5..0%`, `0..2%`, `2..5%`, `5..10%`, `10%+`)
  - tier (`VALUE`, `CONTROL`, `VALUE_CHECK`, etc.)
  - sportKey
  - market
  - bookmaker
  - odds bucket
- Report metrics:
  - count
  - positive CLV rate
  - average EV
  - average CLV
  - median CLV
  - CLV minus EV
  - global `clv ~ computed_ev` linear regression slope/intercept/R^2

## First live result

Command:

```powershell
node src/cli.mjs clv-calibrate
```

Output summary:

- captured rows: 393
- average CLV: -1.02%
- regression slope: +0.9051
- R^2: 0.2537
- diagnostics rows: 36

Key segments:

- VALUE: n=83, beat rate 80.7%, avg CLV +2.18%
- CONTROL: n=307, beat rate 21.8%, avg CLV -1.91%
- 0..2% EV: n=57, avg CLV +0.79%
- 2..5% EV: n=35, avg CLV +3.71%
- 5..10% EV: n=3, too small to interpret
- MATCH_RESULT: n=350, avg CLV -0.74%
- TOTALS: n=43, avg CLV -3.34%

Interpretation:

- The current forward paper data supports a real positive EV-to-CLV gradient.
- VALUE is materially outperforming CONTROL on fast CLV signal.
- The best-supported near-term research band is 0-5% computed EV.
- Do not infer much from 5-10% yet; sample is too small.
- TOTALS should remain research-only until the segment stops underperforming.
- ROI is still secondary until settlement volume is much larger.

Verification:

- `npm test -- test/cli_clv.test.mjs` passed.
- `npm test` passed: 242/242.

## Soccer-only event-market expansion

Goal:

- Increase forward VALUE CLV volume without adding US-style markets.
- Keep `MATCH_RESULT` as the main benchmark.
- Keep every market in separate calibration buckets.

Implemented:

- Added `theodds-sweep --market-profile=soccer-core`.
- The profile filters to `soccer_*` sports only.
- It uses The Odds API Event Odds endpoint for:
  - `h2h`
  - `h2h_3_way`
  - `draw_no_bet`
  - `btts`
  - `double_chance`
- It does not add spreads, team totals, alternate lines, player props, or other
  US-style markets.
- Added safety knobs:
  - `--event-limit=N`
  - `--max-event-credits=N`
- Added normalization:
  - `draw_no_bet` -> `DRAW_NO_BET`
  - `h2h_3_way` -> existing `MATCH_RESULT`
- Extended paper CLV capture so pending soccer `DRAW_NO_BET`, `BTTS`, and
  `DOUBLE_CHANCE` rows call event odds near close. This prevents new research
  rows from becoming uncapturable paper-only rows.

First capped live run:

```powershell
node src/cli.mjs theodds-sweep --market-profile=soccer-core --edge=0 --sample-min-ev=-5 --sample-limit=150 --max-sports=8 --event-limit=4 --max-event-credits=150
```

Result:

- sports: 8
- skippedSports: 0
- auditRows: 807
- VALUE rows: 14
- CONTROL rows: 150
- credits spent: 133
- quota remaining: 15299

Important finding:

- The first live run produced usable audit rows only for `MATCH_RESULT`.
- No `DRAW_NO_BET`/`BTTS`/`DOUBLE_CHANCE` rows passed the current consensus/value
  filters in that sample.
- Interpretation: the code path is ready, but current extra-market coverage or
  book depth is not yet strong enough in the sampled events.

Follow-up:

- Run soccer-core again across more soccer events/leagues, capped by credits.
- If extra-market audit rows remain zero, add a coverage report that records
  raw normalized rows per market before the min-books/value filters.

Verification:

- `npm test -- test/theodds_normalize.test.mjs test/cli_theodds_sweep.test.mjs test/cli_clv.test.mjs` passed: 17/17.
- `npm test` passed: 244/244.

## Balanced VALUE CLV acceleration reports

Implemented:

- Added `node src/cli.mjs research-status`.
- The command is offline-only and writes:
  - `reports/research-status.csv`
  - `reports/research-status.json`
- It reports:
  - VALUE CLV captured
  - VALUE pending without CLV
  - CONTROL CLV captured
  - unique selection count
  - missing VALUE CLV count to 200 and 300
- It always emits explicit rows for:
  - overall/all
  - main/MATCH_RESULT
  - market/MATCH_RESULT
  - market/TOTALS
  - market/DRAW_NO_BET
  - market/BTTS
  - market/DOUBLE_CHANCE

Extended `clv-calibrate`:

- Added `uniqueSelectionCount` to calibration rows.
- Added `main:MATCH_RESULT`, excluding `TOTALS`.
- Added `mainScore` to `clv-calibration.json`.
- Added `sampleWarning=LOW_SAMPLE_N_LT_50` for market buckets with fewer than
  50 captured rows.

Extended soccer-core sweep audit:

- `theodds-sweep --market-profile=soccer-core` now writes
  `reports/theodds-sweep-coverage-*.csv`.
- Coverage rows are pre-filter diagnostics for `MATCH_RESULT`, `DRAW_NO_BET`,
  `BTTS`, and `DOUBLE_CHANCE`.
- Reason buckets:
  - `NO_MARKET`
  - `TOO_FEW_BOOKS`
  - `NO_VALUE`
  - `HAS_VALUE`
- This does not relax `min-books` and does not add US-style markets.

Read-only report run:

```powershell
node src/cli.mjs clv-calibrate
node src/cli.mjs research-status
```

Current status:

- VALUE CLV captured overall: 86
- main MATCH_RESULT VALUE CLV captured: 83
- VALUE pending without CLV: 38
- CONTROL CLV captured: 311
- missing to 200 VALUE CLV: 114
- missing to 300 VALUE CLV: 214
- unique selections overall: 549
- unique selections main MATCH_RESULT: 500

Verification:

- `npm test -- test/cli_research_status.test.mjs test/cli_clv.test.mjs test/cli_theodds_sweep.test.mjs` passed: 16/16.
- `npm test` passed: 247/247.

## 16% Codex window operational runbook

Implemented:

- Added `docs/superpowers/runbooks/2026-06-28-value-clv-collection-runbook.md`.
- Captured the remaining-window operating plan as a durable runbook instead of
  opening a new risky implementation stream.
- The runbook defines the collection cycle:
  - main `h2h` sweep for clean volume
  - capped `soccer-core` exploratory sweep for coverage research
  - frequent `clv --window-minutes=40` capture
  - offline `clv-calibrate` and `research-status` checks
- The runbook freezes the current decision rules:
  - main model score remains `MATCH_RESULT` / `h2h`
  - `TOTALS`, `DRAW_NO_BET`, `BTTS`, and `DOUBLE_CHANCE` stay separate
  - no Telegram threshold changes
  - no live-betting rule changes
  - no stake-sizing changes
  - no production alert scope expansion

Baseline written into the runbook:

- VALUE CLV captured overall: 86
- main MATCH_RESULT VALUE CLV captured: 83
- VALUE pending without CLV: 38
- CONTROL CLV captured: 311
- missing to 200 VALUE CLV: 114
- missing to 300 VALUE CLV: 214
- VALUE avg CLV: +2.18%
- CONTROL avg CLV: -1.88%

Operational priority:

1. Capture CLV near kickoff.
2. Check `research-status`.
3. Run main `h2h` sweeps.
4. Run soccer-core only as capped coverage research.

No production code was changed in this step.

## Live/profit-engine audit

Implemented:

- Added offline `node src/cli.mjs profit-engine`.
- The command writes:
  - `reports/profit-engine-report.csv`
  - `reports/profit-engine-report.json`
- It combines:
  - paper ROI and settled sample size
  - VALUE vs CONTROL CLV separation
  - main MATCH_RESULT VALUE CLV
  - live WebSocket feed efficiency
  - live training/audit/lifetime row counts
  - staking diagnostics from fractional Kelly
  - capital readiness state
  - warnings for missing limits/liquidity evidence
- Extended `forensic-audit` live summary with:
  - market message rows
  - candidate message rows
  - training conversion rate
  - explicit `LIVE_FEED_NO_MARKET_MESSAGES` finding
- Extended `ws-lifetime-probe` to preserve provider liquidity:
  - WebSocket odds `max` now becomes `maxBet`
  - `maxBet` is written to live audit/training rows
  - profit-engine treats live `maxBet` rows as liquidity evidence

Current real report output:

- Profit engine readiness: `RESEARCH_ONLY`
- Paper rows: 765
- Unique selections: 550
- VALUE CLV captured: 86
- Main MATCH_RESULT VALUE CLV captured: 83
- CONTROL CLV captured: 311
- VALUE avg CLV: +2.18%
- CONTROL avg CLV: -1.88%
- Main VALUE MATCH_RESULT avg CLV: +2.28%
- Paper ROI: +86.29%, but only 7 settled rows
- Live feed stats rows: 690
- Live market/odds messages: 0
- Live training rows: 0
- Live liquidity rows: 0

Quant interpretation:

- Prematch VALUE/CONTROL signal remains directionally good.
- Realized ROI is not usable yet because the settled sample is far too small.
- Live shadow is connected but currently not useful for model training: it is
  receiving score/status/welcome messages, not odds market messages.
- Since the official Odds-API.io WebSocket syntax supports the current
  `channels=odds,scores,status`, `markets=ML,Totals`, `sport=football`,
  `status=live` shape, the next live blocker to investigate is provider
  coverage/filter/account-selected bookmakers, not local URL construction.

Verification:

- `npm test -- test/ws_lifetime_probe.test.mjs test/profit_engine.test.mjs test/cli_profit_engine.test.mjs test/forensic_audit.test.mjs` passed: 25/25.

## API-grounded live + calibration reliability

Implemented the exact reliability plan from the official Odds-API.io / The Odds
API documentation.

Baseline:

- Committed prior live/profit-engine diagnostics first:
  - `4b249c5 feat: add live profit-engine diagnostics`

Odds-API.io client / live diagnostics:

- Extended `src/client.mjs` with:
  - `listSelectedBookmakers()`
  - `listLiveEvents({ sport })`
  - extended `listEvents({ from, to, bookmaker, skip })`
  - `getOddsMulti({ includeSeq })`, parsing `X-OddsAPI-Seq`
  - `getOddsUpdated({ since, bookmaker, sport })`
  - `getOddsMovements({ eventId, bookmaker, market, marketLine })`
- Added:
  - `node src/cli.mjs live-preflight`
  - writes `reports/live-preflight.csv`
  - writes `reports/live-preflight.json`
  - writes `reports/live-preflight-eventIds.txt`
- `live-preflight` only uses Odds-API.io. It does not load The Odds API and
  does not mutate selected bookmakers.

WebSocket architecture:

- `scripts/ws-lifetime-probe.mjs` now supports:
  - `--eventIds-file=reports/live-preflight-eventIds.txt`
  - `--lock`
  - `--allow-broad-live` escape hatch
  - REST `/odds/multi?includeSeq=true` startup snapshot
  - `lastSeq` priming from `X-OddsAPI-Seq`
  - REST refresh on `resync_required`
- Broad `sport=football` live is no longer the default path. If there are no
  preflight event IDs, the probe fails closed unless `--allow-broad-live` is
  explicitly passed.
- `scripts/run-live-shadow-probe.ps1` now uses the preflight event ID file and
  lock guard.

Fallback live poller:

- Added:
  - `node src/cli.mjs live-updated-poll`
- Uses Odds-API.io `/odds/updated` with a timestamp within the documented
  one-minute window.
- Writes feed stats and `LIVE_UNCONFIRMED` training rows only when real market
  rows exist.
- It does not load The Odds API, and does not claim confirmed EV.

The Odds API coverage/historical endpoints:

- Extended `src/theodds_client.mjs` with:
  - `getEventMarkets`
  - `getHistoricalEvents`
  - `getHistoricalEventOdds`
- Added:
  - `node src/cli.mjs market-availability`
- Output:
  - `reports/market-availability.csv`
  - `reports/market-availability.json`
- Reason buckets:
  - `NO_EVENT`
  - `NO_MARKET`
  - `TOO_FEW_BOOKS`
  - `MARKET_AVAILABLE`
- Hard cap: `--max-credits` is capped to at most 100.

Historical calibration v2:

- `scripts/historical-calibration.mjs` now supports:

```powershell
node scripts/historical-calibration.mjs --multi-snapshot --markets=h2h --snapshots=24h,6h,1h,10m --leagues=BL1,SA,PD --max-events-per-league=50 --max-credits=8000 --reserve-credits=2000
```

- Multi-snapshot mode is h2h-only.
- It resolves The Odds API historical event IDs first, caches them per match,
  then calls historical event odds per snapshot.
- It does not use totals, spreads, props, alternates, or US-style markets.
- `--reserve-credits` is enforced as a real stop condition when observed quota
  remaining reaches the reserve.

Verification:

- Focused live/API/historical tests: 52/52.
- Full suite: `npm test` passed 264/264 before the reserve-guard follow-up.
- Reserve-guard follow-up: `node --test test/historical_calibration.test.mjs`
  passed 8/8.
- Final full suite after reserve guard: `npm test` passed 265/265.

## The Odds API-first collection block

Goal:

- Treat The Odds API as the primary model/research engine while Odds-API.io live
  remains diagnostic until it emits odds market messages.
- Spend expiring credits where they improve model understanding: forward CLV
  volume, market availability, and h2h historical calibration.
- Keep Telegram thresholds, live betting rules, stake sizing, and production
  alert scope unchanged.

Bug fix found during operation:

- `node src/cli.mjs theodds-sweep --markets=h2h` stopped the whole sweep when a
  single active sport returned The Odds API 422.
- Added regression coverage so explicit `h2h` rejects are skipped and the sweep
  continues to later sports.
- Focused verification: `node --test test/cli_theodds_sweep.test.mjs` passed
  6/6 after the fix.

Operational runs:

```powershell
node src/cli.mjs market-availability --sports=soccer_fifa_world_cup,soccer_usa_mls,soccer_brazil_campeonato --markets=h2h,totals,draw_no_bet,btts,double_chance --max-credits=100
node src/cli.mjs theodds-sweep --edge=0 --sample-min-ev=-5 --sample-limit=300 --max-sports=20 --markets=h2h --regions=eu
node src/cli.mjs theodds-sweep --market-profile=soccer-core --edge=0 --sample-min-ev=-5 --sample-limit=150 --max-sports=12 --event-limit=6 --max-event-credits=250
node src/cli.mjs clv --window-minutes=40
node src/cli.mjs clv-calibrate
node src/cli.mjs research-status
node src/cli.mjs profit-engine
```

Results:

- Market availability wrote 135 rows, spent 26 credits, did not hit cap.
- Main h2h sweep: 20 sports, 4 skipped sports, 2040 audit rows, 70 value
  candidates, 300 controls, 16 credits.
- Main h2h sweep recorded 49 new VALUE paper bets and sampled 230 controls.
- Soccer-core sweep: 12 sports, 0 skipped sports, 1761 audit rows, 50 value
  candidates, 150 controls, 250 credits.
- Soccer-core sweep recorded 41 new VALUE paper bets and sampled 88 controls.
- CLV captured rows increased to 531.
- VALUE CLV captured increased from 86 to 106.
- Main MATCH_RESULT VALUE CLV captured increased from 83 to 103.
- VALUE pending without CLV is now 108.
- CONTROL CLV captured is now 425.
- Missing VALUE CLV to 200 is now 94; to 300 is 194.
- VALUE average CLV is +1.91%; CONTROL average CLV is -2.02%.
- Main VALUE MATCH_RESULT average CLV is +2.26%.
- Profit engine remains `RESEARCH_ONLY`: 7 settled rows, VALUE CLV below 200,
  no live market messages, no live training rows, no liquidity rows.

Quant interpretation:

- The forward CLV separation survived the volume increase.
- The strongest actionable research path remains main h2h / MATCH_RESULT.
- Soccer-core is useful for coverage diagnostics but has not promoted
  DRAW_NO_BET, BTTS, or DOUBLE_CHANCE into the main score.
- Continue capturing CLV frequently; new VALUE rows are less useful until their
  closing line is captured.

## Historical h2h credit block

Goal:

- Execute the planned 4k-8k historical credit spend before reset.
- Keep it h2h-only and multi-snapshot.
- Use historical data only for fair-probability calibration, not for
  Stoiximan/Novibet strategy backtesting.

First block:

```powershell
node scripts/historical-calibration.mjs --multi-snapshot --markets=h2h --snapshots=24h,6h,1h,10m --leagues=BL1,SA,PD --max-events-per-league=50 --max-credits=6000 --reserve-credits=2000
```

Results:

- BL1 returned 135 finished outcomes; pulled 50 matches; calibrated 45 events;
  spent 1800 credits.
- SA returned 0 finished outcomes; spent 0 credits.
- PD returned 171 finished outcomes; pulled 50 matches; calibrated 45 events;
  spent 1800 credits.
- Total: 100 matches pulled, 90 calibrated events, 3600 historical credits.
- Quota remaining after the run: 10979.
- consensus_power_median validate Brier: 0.5802 vs baseline 0.6548.
- consensus_power_median validate logLoss: 0.9705 vs baseline 1.0815.

Second block:

```powershell
node scripts/historical-calibration.mjs --multi-snapshot --markets=h2h --snapshots=24h,6h,1h,10m --leagues=BL1,PD --from=2026-01-01 --to=2026-06-28 --max-events-per-league=30 --max-credits=2500 --reserve-credits=2000
```

Results:

- BL1 returned 171 finished outcomes; pulled 30 matches; calibrated 26 events;
  spent 1040 credits.
- PD returned 209 finished outcomes; pulled 30 matches; calibrated 28 events;
  spent 1120 credits.
- Total: 60 matches pulled, 54 calibrated events, 2160 historical credits.
- Quota remaining after the run: 8759.
- Quota remaining after final CLV/report pass: 8753.

Combined historical block:

- Historical credits spent: 5760.
- Matches pulled: 160.
- Calibrated events: 144.
- Reserve guard remained active; neither run stopped by reserve or credit cap.
- This completes P5's intended 4k-8k h2h-only multi-snapshot spend.
